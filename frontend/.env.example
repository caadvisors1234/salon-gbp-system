# Docker: these are injected from root .env via docker-compose.
# Standalone dev (npm run dev outside Docker): uncomment and fill in.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
VITE_API_BASE=/api

