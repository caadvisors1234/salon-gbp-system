services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - media_data:/data/media
    depends_on:
      - api
      - frontend

  api:
    build:
      context: ../backend
    env_file:
      - ../.env
    volumes:
      - media_data:/data/media
    depends_on:
      - db
      - redis

  worker:
    build:
      context: ../backend
    command: celery -A app.worker.celery_app.celery_app worker --loglevel=info --concurrency=2
    env_file:
      - ../.env
    volumes:
      - media_data:/data/media
    depends_on:
      - db
      - redis

  beat:
    build:
      context: ../backend
    command: celery -A app.worker.celery_app.celery_app beat --loglevel=info
    env_file:
      - ../.env
    volumes:
      - media_data:/data/media
    depends_on:
      - redis

  frontend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ../frontend:/app
    env_file:
      - ../.env
    environment:
      - VITE_API_BASE=/api
    # Vite requires VITE_ prefixed env vars at process start time.
    # Using shell export ensures vars are available when 'npm run dev' launches.
    command: >
      sh -c "
        export VITE_SUPABASE_URL=$$SUPABASE_URL
        export VITE_SUPABASE_ANON_KEY=$$SUPABASE_ANON_KEY
        npm install && npm run dev -- --host 0.0.0.0 --port 5173
      "

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: salon_gbp
      POSTGRES_USER: salon_gbp
      POSTGRES_PASSWORD: salon_gbp
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine

volumes:
  postgres_data:
  media_data:

